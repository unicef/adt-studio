---
title: "Extract content"
description: "Turn the pages of your PDF into structured, editable content."
icon: ScanText
---

Extraction is the step where ADT Studio reads your PDF and converts it into structured content — text, figures, and layout — that all later steps build on. This is where the AI does its first and most important pass over your book.

Before extraction runs, you configure how it should treat your document. This configuration happens in a few sub-steps.

{/* dynamic: this entire page mirrors app screens closely. Verify all step names, preset names, and field labels against the current UI before publishing, and re-verify after app updates. */}

## Choose a preset

A preset is a starting configuration that matches the kind of document you are converting. It pre-selects sensible settings so you don't have to configure everything manually.

{/* TO VERIFY: the exact preset names and descriptions currently offered in the app. List each preset with a one-line example of when to use it, using the app's exact terms. */}

Presets are a starting point, not a commitment. After choosing one, you can still adjust any individual setting.

## Basic information

Here you confirm the essential facts about your book: its title, the language of the source document, and other identifying details that will travel with the ADT.

{/* TO VERIFY: exact fields shown in the Basic information step. */}

## Visual layout

This step tells ADT Studio how your document is visually organised, so extraction can interpret the pages correctly. ADT Studio will already suggest what fits best for the preset you chose — in most cases you can accept the suggestion.

{/* TO VERIFY: exact options in the Visual layout step and how the suggestion behaviour works. */}

## Content processing

Here you choose which AI-powered processing runs on your content during extraction.

{/* TO VERIFY: exact toggles/options in the Content processing step, with the app's exact terms. */}

You do not need to enable everything now. Features you skip here can generally be run later — see [Enhance your ADT](/docs/captions).

## Languages

If your ADT should exist in more than one language, you select the target languages here. Translation happens with AI support and each additional language adds processing (and therefore cost).

{/* TO VERIFY: exact behaviour of the Languages step — whether it can be changed later, and how it interacts with the Language enhancement step. */}

## Run the extraction

Once your PDF is imported and your settings are configured, you extract content into your ADT. This process happens page by page — it can take a few minutes depending on the length of your document. You can follow the progress in the interface.

When extraction finishes, your book exists as structured content inside ADT Studio — ready for the next step: [Sectioning](/docs/sectioning).

| |
|---|
| **Good to know:** if you change a setting and run extraction again, ADT Studio only redoes the work affected by your change. Everything else is reused instantly from the saved results, at no extra cost. And nothing is lost — earlier results are kept and you can go back to them. |
