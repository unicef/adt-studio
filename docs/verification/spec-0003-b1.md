# SPEC-0003 B1 implementation evidence

Status: B1 safe slice implemented locally; specification and ADR 025 remain in review.
No approval or release verification is implied. Source issue: #708.

## Baseline and dependencies

Target: PR #884, `spec/708-sectioning-mode-lifecycle`. Published head:
`04250fda6ba187b5712961e21c1dd3504aaedfe6`. Develop:
`737d3314` (includes SDD scaffolding #878). The spec commit was rebased alone;
the original head remains at `backup/spec-0003-before-implementation-20260925`.
The main checkout and its uncommitted drafts were left intact.

#879 and #880 contain specifications, not implemented freshness/preservation.
#886 is a descendant documentation branch, not a CLI implementation prerequisite.
No descendant is merged into this branch. #692 by @KaReeeeeeeeEM is merged;
its runtime mode validator and existing cache eviction/retry are reused unchanged.
#884 has no posted review comments or submitted reviews at inspection.

The baseline source used independent YAML writes, destructive pre-run clears, and
separate API and DAG render paths. There is no shared book writer admission.
The implementation adds that admission to the storage boundary and shares it
across supported writers. Until #880 provides protection, existing Sectioning
regeneration must fail closed; a mode change is never replacement permission.
The conservative completion invalidation and unsafe-run rejection are the
documented rollout fallback, not implementation of SPEC-0001 or SPEC-0002.

## Pre-edit acceptance map

This map was recorded before implementation; final results appear below.

| AC | Current behavior / change required | Concrete verification |
|---|---|---|
| 1 | YAML-only save; invalidate derived Sectioning closure | Real DB dynamic → page, all affected step rows absent |
| 2 | No inverse invalidation | Real DB page → dynamic, identical closure |
| 3 | Preserve Extract throughout transition | Snapshot Extract rows, pages and images before/after |
| 4 | No transition preservation contract | All entity versions/pointers and media byte hashes unchanged |
| 5 | Compare effective modes | Same-mode update leaves statuses unchanged |
| 6 | Unrelated save must remain unrelated | Unrelated override update leaves statuses unchanged |
| 7 | No serialized mode save | Held writer and running-step rejection, no mutation |
| 8 | Selector persists immediately | Retained/completed output confirmation component test |
| 9 | No confirmation cancellation | Cancel test counts zero mutation requests |
| 10 | Optimistic selection survives failure | Error test restores authoritative value and displays error |
| 11 | Incomplete query refresh | Success test asserts affected query invalidations |
| 12 | No persisted-data guard | Missing latest value pure validator + runner test |
| 13 | No persisted-data guard | Empty sections pure validator + runner test |
| 14 | Only generation-time validator | Multiple sections pure validator + runner test |
| 15 | Cleanup precedes validation | HTTP/runner failure preserves versions/media; zero rendering calls |
| 16 | No shared guard | Valid single-section fixtures pass pure preflight; other prerequisites explicit |
| 17 | Dynamic accepts multi-section | Dynamic fixture passes unchanged |
| 18 | Historical data unvalidated | Historical malformed/null/non-contiguous persisted fixtures |
| 19 | Separate branches bypass validation | API, queued, targeted, DAG, fixed and reflowable entry tests |
| 20 | New copy needed | Lingui extraction, five locale completeness, lint |
| 21 | No dependency needed | Package/lockfile diff is empty |
| 22 | Raw override writes; invalid mode silently defaults | Global inheritance, override removal, omitted default and invalid input tests |
| 23 | No recoverable YAML/DB publication | Real filesystem/DB fault injection and journal recovery boundaries |
| 24 | Check-then-write races | Independent writer processes, queued mode reload and post-preflight edits |
| 25 | No structured bounded summary | >20 failures, exact total and full structured details, bounded display |
| 26 | #692 merged | Existing cached-invalid eviction and valid reuse tests at transport boundary |

## Verification results

All results are development evidence, not human acceptance or release verification.

Evidence suites (repository-relative paths):

- **L**: `apps/api/src/services/sectioning-lifecycle.test.ts` — real SQLite/files, effective modes, retained history, active task/process admission, journal fault injection, actual child-process termination, part window and publication conflicts.
- **R**: `apps/api/src/services/sectioning-preflight-runners.test.ts` — HTTP, queue, single/batch targeted, real fixed/reflowable stage boundaries, cancellation and safe CLI/DAG rejection.
- **P**: `packages/pipeline/src/__tests__/sectioning-preflight.test.ts` — pure mixed/non-contiguous/malformed/legacy fixtures and bounded errors.
- **U**: `apps/studio/src/hooks/use-sectioning-mode-confirmation.test.tsx` — actual dialog and query/mutation lifecycle, cancel, failure, duplicate/pending and busy states.
- **C**: `packages/pipeline/src/__tests__/page-sectioning.test.ts` — production `sectionPage` + real disk cache + real LLM client/registry, stubbed provider transport with counted requests. Corrupt cached multi-section output is evicted, feedback reaches the next request, and valid repeats make no additional provider request.
- **E**: `apps/api/src/services/export-service.test.ts` — retained lifecycle metadata, excluded process ownership, admission through large archive reads/cancellation.

| AC | Status | Evidence and limits |
|---|---|---|
| 1 | Passed locally | L: Dynamic → By Page clears the PIPELINE-derived completion closure, including translation. |
| 2 | Passed locally | L: inverse transition clears the same closure. |
| 3 | Passed locally | L: Extract step rows and page/image database snapshots are unchanged. |
| 4 | Passed locally | L: all seeded entity versions/current pointers and media bytes are unchanged; E: lifecycle survives project export. |
| 5 | Passed locally | L: same effective mode leaves completion unchanged. |
| 6 | Passed locally | L: unrelated update leaves completion unchanged. |
| 7 | Passed locally | L: running rows, live task and independent process reject without configuration/content mutation; HTTP returns BOOK_BUSY. |
| 8 | Passed locally | U and browser: retained stale output triggers the existing confirmation dialog. |
| 9 | Passed locally | U counts zero mutation calls; browser cancellation retained Dynamic and API log contained no PUT. |
| 10 | Passed locally | U: rejected update leaves authoritative mode and shows error. Browser: a separate live writer caused HTTP 409, the reason appeared, and By Page remained selected. |
| 11 | Passed locally | U: query invalidations; browser: By Page persisted and Storyboard changed from done to incomplete. |
| 12 | Passed locally | P: missing latest value; R: shared preflight boundary. |
| 13 | Passed locally | P: empty latest sections. |
| 14 | Passed locally | P/R: multi-section latest result. |
| 15 | Passed locally | R: zero renderer calls on failure; L/R: prior rendering history retained; cancellation publishes no partial batch. |
| 16 | Passed locally | P/R: valid By Page pages publish fixed and reflowable versions when lifecycle is current. Unknown provenance remains a separate blocking prerequisite. |
| 17 | Passed locally | P and page-edit/stage-runner regressions: Dynamic multi-section input remains valid when provenance is known. |
| 18 | Passed locally | P/L/R: persisted inconsistencies checked without a config transition; no historical tree conversion. Unknown provenance fails closed. |
| 19 | Blocked for full flow | API/queue/targeted/fixed/reflowable share the guard. Existing-book CLI/DAG is safely rejected before source copy; positive existing-book resume/regeneration awaits preservation/admission prerequisites. |
| 20 | Passed locally | Lingui extraction and five completed catalogs; Studio lint. No new raw UI strings. |
| 21 | Passed locally | No package manifest or lockfile changes. |
| 22 | Passed locally | L: inherited mode, override removal, omitted default and invalid mode. Empty override map is atomically persisted as YAML `{}`. |
| 23 | Passed locally | L: file errors, SQLite transaction abort, child exits after journal/YAML/first status delete/lifecycle/ack. Recovery old/new hash decisions and duplicate retry preserve content. OS power-loss and other-platform checks are not implied. |
| 24 | Passed locally | L: task/process admission, effective config/active Sectioning version/rollback conflicts; R: queued execution reload and cancellation. E: archive read admission. |
| 25 | Passed locally | P: 30 failures, exact total, full structured data, only 20 displayed identities; HTTP/SSE feed translated Studio formatter. |
| 26 | Passed locally | C: actual provider boundary request counts for invalid cache retry and valid cache reuse, preserving #692 implementation. |

### Commands and observations

Final verification on 2026-09-25 (Node 22.23.2, pnpm 10.32.1, macOS arm64):

| Command/check | Result |
|---|---|
| `PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH pnpm test --maxWorkers 4` | Passed: 281 files, 3,654 tests; includes pretest build. |
| Final preview-status fix: `pnpm exec vitest run apps/api/src/services/sectioning-lifecycle.test.ts apps/api/src/routes/stages.test.ts apps/api/src/services/book-service.test.ts --maxWorkers 3` | Passed: 3 files, 81 tests after that final read-model change. The full suite was not repeated after this two-line status refinement. |
| `pnpm typecheck` | Passed, including after the final read-model change. |
| `pnpm --filter @adt/runtime typecheck` | Passed. |
| `pnpm lint` | Passed: zero errors, eight unused-suppression warnings. No baseline lint comparison is claimed. |
| `pnpm --filter @adt/studio extract` | Passed: 3,611 messages, zero missing translations in es/fr/pt-BR/sq. |
| `pnpm build` | Passed (also run through pretest). |
| `pnpm --filter @adt/desktop build:unpack` | Passed on final runtime code: macOS arm64 unpacked package. Ad-hoc signature only; notarization skipped. |
| `git diff --check` | Passed. |
| Spec/INDEX metadata, all 26 evidence rows and relative documentation links | Passed; spec in-review, no approvers, ADR proposed. |

Local code commits: `3fcd2148`, `ed3a8ba0`, `c7612400`. These are on the
existing PR branch in an isolated checkout. Nothing is pushed or merged.
The remote head remained `04250fda6ba187b5712961e21c1dd3504aaedfe6` when
checked before delivery; the local branch is based on develop `737d3314`.


- Baseline: `pnpm build` passed. Default Git shim caused 22 failures in two Git-dependent files; rerunning those 38 tests with CommandLineTools Git passed. The other 3,593 baseline tests passed.
- Focused lifecycle/cache/export/UI suites: 120 tests passed before the final integration additions.
- Updated contract regression suites: 164 tests passed, including positive fixed/reflowable publication and cancellation.
- First integrated `pnpm test`: 3,636 passed / 11 failed. Seven assertions still expected the replaced deletion/provenance behavior and were corrected to test retention or explicitly current fixtures; four export/package cases timed out. No claim these timeouts were baseline failures. The final bounded run above passed. A preceding bounded run passed 3,653 tests and caught one test expecting immediate removal of a completed TaskService item; that test now checks completion while respecting its existing 30-second retention.
- Browser smoke uses a disposable synthetic book, not a representative partner publication. Confirm/cancel/pending, persisted mode, retained stale notice and stage status refresh were exercised through Studio → HTTP → SQLite/YAML.
- Docker verification blocked: `docker info` reports no running daemon.
- `pnpm lint:invariants` unavailable: repository has no such command. Executable regressions above enforce this increment's invariants.

## Scope, rollout and remaining decisions

This is **only the documented B1 safe slice**, not full SPEC-0003 completion.
It implements guarded mode publication, durable transition recovery, conservative
completion invalidation, persisted-data preflight and Studio confirmation.
B2 modes and cross-page merge are untouched.

Existing semantic Sectioning (including unknown/manual origin or inactive history)
is not regenerated. Existing extracted books cannot be re-extracted by the CLI/DAG
fallback. Storyboard with unknown lifecycle provenance is rejected even when its
section count happens to be valid. A fresh Sectioning run can establish provenance;
a mode change alone cannot. Thus changing mode on an existing book can leave it
inspectable but unable to regenerate until SPEC-0002 protection is implemented.
This limitation must remain visible at review and rollout; the selector does not
promise that this branch alone can complete an existing-book mode conversion.

The lifecycle file is a B1 completion/mode marker, not a replacement for SPEC-0001
freshness or SPEC-0002 provenance. The journal stores version, old/new YAML hashes,
new mode, derived affected steps and prior completion rows. Under the shared
book-local lease, old hash means no publication occurred; new hash completes
invalidation; any other hash fails closed. Readers do not trust unfinished journals.
The lease is shared by supported API writers, tasks, runners and CLI, uses canonical
book paths and does not steal a live or unknown owner based on elapsed time.
Recovery of a killed transition owner removes only its WASM mutex; SQLite journals
remain for SQLite recovery. A foreign/unknown owner or interrupted lock-reaper fails
closed for operator inspection. Independent global-config edits are detected again
at rendering publication, and active version pointers are part of that comparison.

The two existing spec approval decisions remain pending: B1/B2 identity ratification
and storage-maintainer review of journal/admission representation. No approver or
release-verified status is fabricated. No acceptance criterion has been rewritten.
No alternative implementation PR was modified; #692 work is reused with attribution.

Do not deploy older writers against an unfinished journal, silently delete locks,
or downgrade past recovery support. No representative-book provider run, Windows/
Linux installer smoke, signed/notarized release or Docker image acceptance is claimed.
